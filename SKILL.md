---
name: travel-guide-skill
description: 仅在用户显式调用时，把一个全新、连续任务中的旅行想法推进为可执行行程和经实际成品 Review 的手机旅行攻略；支持目的地未知或已知，不处理中途接手或跨会话恢复。
---

# Travel Guide Skill v0.1

把一个全新、连续任务中的旅行想法推进为现实可执行的方案。默认交付适合手机现场查看的视觉攻略；用户明确要求时，改为纯文字或混合交付。

## 工作边界

- 仅处理用户主动调用后的当前连续任务，不实现自动触发、中途接手、跨会话恢复或专门的后续更新工作流。
- 模型主导研究、比较、推荐、行程组合、复杂景点深化、内容拆分、视觉生成和普通修复。
- 默认把研究与中间结果留在当前任务上下文。只有资料复杂、来源很多或确有复用价值时才创建中间文件。
- 不建立固定城市模板、页数、视觉风格、地图形式、画布尺寸、组件、搜索引擎、地图服务、来源清单、状态系统或多层审批。

## 推进与决策

先提取出发地、日期与可用时间、预算、兴趣、节奏、同行人及行动限制，以及已订酒店、交通、票务、固定活动、必去／不去和用户明确确认的事实。禁止或只允许的交通方式、用户锁定的门到门链路与换乘、行李数量和携带方式、不寄存或不折返取行李、已接受或不能接受的费用方案也都是规划约束；不能因为其他方案通常更方便就静默替换，发现现实冲突时明确说明。

只在答案会显著改变目的地、预算、固定活动、核心体验、总体行程方向或整体视觉方向时集中询问用户。普通景点排序、交通细节、用餐、缓冲、必要 Plan B、内容拆分和视觉实现由模型决定。能研究的客观事实不要反问用户。

“你决定”“按你推荐的来”“直接做完整方案”等一般授权，只延续到当前连续任务中的普通后续决定。模型说明所选方向、关键假设和主要取舍后继续，不逐阶段重复确认；但一般授权不能跳过核心景点与体验范围确认，也不自动等于用户授权整体视觉方向。只有用户明确表达“核心景点和体验范围也由你决定，不需要我确认”的等价意思时，模型才能代为确定该范围；只有用户明确授权整体视觉方向也由模型决定时，才能不等待用户亲自确认，此时仍须由模型和独立 Reviewer 查看实际代表图后通过。用户沉默不等于任何授权。

## 自适应工作流

### 1. 收敛并确认旅行范围

根据目的地状态选择一个入口：

- **目的地未知：** 研究少量有区分度且现实可行的候选，比较匹配度、核心体验、到达与通勤、时间、预算、体力、天气、人流或预约代价；给出首选、备选和关键取舍，不只罗列热门地点。目的地确定后继续执行下面的核心范围判断。
- **目的地已知：** 直接进入景点、活动与城市体验的发现和范围判断。

目的地已经确定，而用户尚未明确完整的核心景点与体验范围时，必须先研究并给出一份精炼、有判断的建议，区分：

- 推荐进入核心行程；
- 条件合适时可以安排；
- 不建议纳入本次旅行，或用户已经排除。

对主要候选只说明真正影响选择的内容：实际体验、适合或不适合的原因、大致用时、与其他地点的空间组合，以及预约／票务、客流、费用和体力代价；必要时再说明它是否值得增加一天或一晚、会怎样影响酒店区域和行程骨架，以及是否值得在最终视觉攻略中做知识深化。

随后集中请用户把主要候选标为“必去／可以安排／不去／仍需比较”，并停止推进。确认前不得最终确定实际旅行天数和住宿晚数、最终酒店、最终城际交通组合、详细逐日行程或完整视觉攻略。用户已明确给出完整必去与不去范围，或已明确授权核心范围也由模型决定时，才跳过这次确认；一般授权和用户沉默都不能跳过。

核心范围确定并形成行程结论后，用户若又排除、增加或替换会影响结构的核心体验，只重新打开受影响范围，但必须重新判断实际旅行天数、住宿晚数、到达日和返程日结构、酒店区域、门到门交通、每日空间组合、其他景点停留、预约购票、最后一天衔接和最终视觉内容范围。明确哪些既有结论失效、哪些仍成立，并判断应以其他高价值体验替代还是缩短旅行；不得只删改原日程一行，也不得为维持原天数作低价值填充或无关地重启目的地发现和全部研究。

### 2. 研究并组合可执行行程

- 对开放时间、临时关闭、预约、票务、交通和活动规则等时效性事实，优先采用当前且权威的来源；按事实的重要性和时效在必要时交叉核实，但不绑定固定工具或来源清单。
- 以用户已确认的预订、交通链路、换乘、行李处理、费用边界、必去与不去为锚点。只有发现现实冲突时才显式提出取舍，不用默认方案替换。
- 先把不可移动的活动、交通和预约放入时间轴，再按地理位置、开放时间、通勤、节奏和同行人安排每日主线；加入合理的用餐、休息、排队和交通缓冲，只在真实风险存在时设计 Plan B。
- 核心景点若存在明显预约或抢票失败风险，主动核实主要购票路径和合法、现实的备选入场路径；确认收费特展、联票、讲解票等在旅行日期是否实际销售、能否独立入场、包含哪些区域与权益，以及销售时间、实名、退改、入口和时段要求。不得由“有收费票”推断其包含基本陈列、完整入馆资格或能替代免费预约，也不得把当前展览外推为未来仍开放。
- 给出一个推荐方案及主要取舍；区分当前可采用的信息与需临近出发更新的信息，无法可靠核实时保留不确定状态和复核动作。

### 3. 深化复杂地点并组织内容

按实际复杂度决定深化范围。复杂地点的最终攻略同时承担“怎么执行”和“如何理解”，不能只给开放时间、入场、停留时长和简单顺序：

- **博物馆：** 按需组织整体路线、重点展厅或主题、重点藏品或关键历史物证的准确名称、年代与背景，说明为什么重要、现场观察什么；可靠时说明当前是否展出及所在区域，并给出时间不足时的取舍。
- **遗址、古建筑、城墙、历史街区和宗教场所：** 按自身特点说明历史脉络、空间或建筑结构、关键遗存与节点、现场观察方法、真实证据如何理解，以及时间不足时的取舍。
- **其他复杂地点：** 根据类型补充入口、内部顺序、预计停留、重点内容与观察点、预约规则和现场易错点。

不得把馆藏对象写成当天一定展出，不得把想象的文物或建筑外观当成真实对象，也不得用装饰图片或无解释作用的素材占位块代替知识内容。自主决定总览、每日、专项或深层内容如何拆分，不按天数、景点数或参考成品固定图片数量。

### 4. 生成并验证实际成品

默认生成适合手机竖向自然浏览、重要执行信息直接可见的视觉攻略，但不固定画布尺寸。用户明确选择纯文字或混合交付时遵从其选择。

用户提供视觉参考时，先提炼其明确认可的体验维度，例如目的地和景点是否一眼可识别、图像／地图／路线／文字能否成为同一视觉作品、旅行感与视觉叙事、单图有效信息、画布利用、手机扫读、图片的解释作用及不同内容是否采用合适的不同构图。参考成品不作为旅行事实源，不自动决定页数、城市风格或旧行程与版式，但这些认可维度必须实际进入代表图判断和 Reviewer 对比，不能只在说明中声称“已参考”。

同时满足以下条件时，代表性视觉验证必须执行：最终会生成共享整体视觉语言的多图成品；用户提供了重要视觉参考或把视觉完成度列为核心验收目标；错误方向会造成明显批量返工。

- 必须先生成覆盖本次不同高风险内容类型的实际图片，不能用风格说明、设计系统、页面目录或文字线框代替。若任务确有总览空间关系和复杂知识页，代表图通常分别覆盖这两类；这不是固定数量或模板。
- 用户未明确授权整体视觉方向由模型决定时，展示实际代表图并等待确认；未通过时只修改代表图，确认前不得批量生成。
- 用户明确授权整体视觉方向也由模型决定时，由模型和本任务唯一的独立 Reviewer 查看实际代表图及用户参考；方向成立后才能批量继续。
- 未同时触发上述条件，或代表方向已经通过时，直接生成完整候选。图片数量、顺序、视觉风格、地图形式、构图和实现方法由模型根据任务决定。

### 5. 独立 Review、修复与交付

只使用一个未参与当前候选生成的独立 Reviewer Agent，并在代表图、完整候选和修复复查中复用同一个 Reviewer。向其提供用户目标与使用场景、已确认条件、必要参考成品，以及用户实际会看到的代表图或完整候选；不要用 Generator 自评、源码意图、坐标说明或既有 PASS 代替实际成品。

Reviewer 除了检查事实、执行遗漏、错字、歧义、遮挡、错锚和手机可读性，还要判断整体视觉方向是否成立。以下可构成方向失败：成品像编辑报告、咨询简报、Dashboard、Notion 卡片、通用 PPT 或重复卡片模板；目的地或当前景点不能一眼识别；图片只是装饰；不同页面机械复用同一构图；存在大面积无功能空白而正文仍过小或关键信息缺失；总览未形成整趟旅行的空间骨架；封面与内页像两套产品；必须逐段阅读才能恢复上下文；或在用户明确认可的视觉维度上明显低于参考成品。

Reviewer 只说明问题位置、具体表现、为何影响使用，以及属于局部问题还是整体方向问题；不得规定具体布局、配色、组件或修图技术，也不得自行增加页面、景点或旅行内容。

- 有局部实质问题：Generator 定点修复，Reviewer 检查新的实际成品是否解决原问题并避免明显新问题。
- 无实质问题：立即停止，不预设 Review 轮数。
- 整体方向未成立：受共享方向影响的旧批次失效，回退到代表性视觉阶段，只重做代表图，不在完整批次上填空白、加装饰或逐页打补丁；按用户是否明确授权视觉方向完成相应确认或独立 Review，方向成立后再重新扩量。

最终按清楚的现场使用顺序交付可直接查看的成品；视觉交付须明确图片查看顺序，并在攻略内或最终提醒中列出出发前需更新的信息。用户只提出局部问题时，仅重开受影响范围并复查新的相关成品。若当前环境无法生成实际视觉成品或提供真正独立的 Agent，明确说明限制和已完成范围，不把自检宣称为独立 Review。

## 按复杂度裁剪

- **简单任务或完整范围：** 核心范围已完整给出、经用户确认或获专项授权后，简单任务可合并需求理解、研究和行程组合；只深化必要地点。不满足强制视觉门条件时直接生成少量完整候选并进入有停止条件的独立 Review，不创建事实包、专题文件、代表页门禁或多轮审批。
- **复杂任务：** 按需增加事实研究、复杂地点专项深化和代表性视觉验证，但仍采用最小充分拆分，不增加另一套固定流程、固定 Agent 编排或额外 Reviewer。

## 五条 Guardrails

1. 用户已锁定或明确确认的条件不得被搜索、工具、模型推断或 Reviewer 静默覆盖；发现现实冲突时必须显式提示。
2. 未公布、待确认或需临近出发更新的信息不得伪造为确定事实；在最终攻略或提醒中保留其状态。
3. 事实经压缩、改写、拆分、合并或视觉化后，不得改变时间、顺序、空间关系、数量、因果或动作状态。
4. 参考成品不作为旅行事实源，也不得机械复制其城市风格、结构、页数或组件；只提取用户明确认可的体验或视觉目标。
5. Review 必须检查用户实际会看到的最终输出；修复后必须检查新的实际成品，不得用 Generator 说明或局部检查结论代替。
